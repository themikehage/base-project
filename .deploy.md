# .deploy.md — Deployment Guide ({{PROJECT_NAME}})

> **git-ignored** — contains production URLs, UUIDs, and environment secrets.

{{PROJECT_NAME}} deploys on **{{PLATFORM}}**.

---

## 1. Production Infrastructure

| Parameter | Value |
|---|---|
| **Production URL** | `https://{{PROJECT}}.your-domain.com` |
| **Health Check** | `https://{{PROJECT}}.your-domain.com/api/health` |
| **Coolify Instance** | `https://coolify.your-domain.com` |
| **Application UUID** | `{{UUID}}` |
| **Git Repository** | `git@github.com:{{GITHUB_USER_OR_ORG}}/{{PROJECT}}.git` (branch `main`) |

---

## 2. Standard Redeploy Flow

### Step 1: Validate Locally
```bash
pnpm typecheck
pnpm build
```

### Step 2: Commit and Push
```bash
git add .
git commit -m "feat(scope): description"
git push origin main
```

### Step 3: Trigger Deploy
```bash
bun run .agents/skills/coolify/scripts/coolify-cli.ts deploy {{UUID}}
```

### Step 4: Verify Health
```bash
bun run .agents/skills/coolify/scripts/coolify-cli.ts health https://{{PROJECT}}.your-domain.com
```

---

## 3. Environment Variables

| Key | Description |
|---|---|
| `NODE_ENV` | `production` |
| `PORT` | `3000` |

---

## 4. OAuth Redirect URIs

| Provider | Callback URL |
|---|---|
| **Provider** | `https://{{PROJECT}}.your-domain.com/auth/callback` |
